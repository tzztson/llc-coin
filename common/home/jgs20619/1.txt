!pip install python-gitlab
!pip install pandas
import gitlab
import pandas as pd
import os
gl = gitlab.Gitlab(url='https://gitlab.com/', private_token='glpat-DptrTHzx-SKPQ5HsnbgF')
project_name_with_namespace = "skawsdevops/stockscanner"
project = gl.projects.get(project_name_with_namespace)
items = project.repository_tree(path='company', ref='main')
# print(items)
df = pd.DataFrame(items)
print(df)

The One DevOps Platform | GitLab
From planning to production, bring teams together in one application. Ship secure code faster, deploy to any cloud, and drive business results.